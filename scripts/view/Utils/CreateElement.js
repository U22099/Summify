export const CreateElement = (type) => {
  
  // create new element with the type argument and assign it to a variable 
  const element = document.createElement(type)
  
  //return the variable
  return element;
}
